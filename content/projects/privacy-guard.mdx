## Local-first is a security stance

If the tool’s purpose is to stop leaks, the default path cannot be “send everything to the cloud.” Local checks and **sanitized outbound payloads** are non-negotiable primitives.

## Provider flexibility without rewriting rules

Hot-swappable providers let the extension evolve with model availability while keeping enforcement logic stable.
